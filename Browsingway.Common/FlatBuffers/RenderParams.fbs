attribute "fs_serializer";

namespace Browsingway.Common.Ipc;

table RenderParams (fs_serializer) {
    CefAssemblyDir : string (required);
    CefCacheDir : string (required);
    DalamudAssemblyDir : string (required);
    DxgiAdapterLuidHigh: int;
    DxgiAdapterLuidLow: uint;
    IpcChannelName : string (required);
    KeepAliveHandleName : string (required);
    ParentPid : int;
}